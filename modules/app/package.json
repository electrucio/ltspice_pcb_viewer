{
  "name": "app",
  "version": "0.1.0",
  "description": "LTspice ↔ KiCad cross-probe application: upload an LTspice schematic and a KiCad project (schematic + PCB), map nets/components with synchronized highlighting, and download a read-only single-file HTML viewer compatible with old iOS Safari.",
  "type": "module",
  "private": true,
  "scripts": {
    "build:viewer": "vite build --config vite.viewer.config.ts",
    "dev": "npm run build:viewer && vite",
    "build": "npm run build:viewer && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
